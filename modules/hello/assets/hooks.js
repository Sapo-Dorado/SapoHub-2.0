// LiveView JS hooks contributed by the hello module.
// Nix composes every enabled module's hooks into core's bundle
// (core/assets/js/module_hooks.js). Keep hooks framework-free so the
// future Capacitor app can reuse them.
export default {};
