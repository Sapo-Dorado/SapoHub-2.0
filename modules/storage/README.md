# Storage

A SapoHub util module. Implements the `SapoKit.Module` contract in
`lib/storage/module.ex` — see `modules/hello` for a fully worked example
and `docs/module-authoring.md` for the contract reference.
